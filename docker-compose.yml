services:
  app:
    container_name: visualization_receiver
    image: kenyahmis/visualization_receiver:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HOST_PORT:-5000}:${PORT:-5000}"
    env_file:
      - .env
    restart: unless-stopped
